name: ${COMPOSE_PROJECT_NAME:-omni}

# AC-11: com o profile automation, mem_limit soma ~5,25 GiB de TETO (nao alocacao).
# Se um servico estoura o limit, o kernel OOM-killa e o `restart: unless-stopped`
# ressuscita — auto-recuperacao em vez de derrubar a VPS.

# AC-16: Log rotation. Sem isso o json-file default cresce sem limite e enche o disco
# da VPS (~6GB compartilhada). Mudanca de logging so aplica ao RECRIAR o container
# (up -d --force-recreate) — ver docs/DEPLOY_VPS.md secao Monitoracao.
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    mem_limit: 1g
    mem_reservation: 256m
    cpus: 2.0
    logging: *default-logging
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    networks:
      - app

  api:
    # Imagem do registry (GHCR). IMAGE_TAG recebe o SHA do git (nao "latest"):
    # garante que o byte-a-byte testado em staging seja o promovido pra prod.
    # Fluxo de prod usa `pull` + `up --no-build`; a secao `build:` abaixo so
    # serve pra build local de emergencia/dev.
    image: ${API_IMAGE:-ghcr.io/mikewade2k16/omni-api}:${IMAGE_TAG:-latest}
    build:
      context: ./back
      dockerfile: Dockerfile
    restart: unless-stopped
    mem_limit: 512m
    mem_reservation: 128m
    cpus: 2.0
    # AC-16: janela maior que o anchor (o slog loga cada request; 30MB seria curto p/ depurar).
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    environment:
      APP_NAME: ${APP_NAME:-omni-api}
      APP_ENV: production
      APP_ADDR: :8080
      CORE_V2_ENABLED: ${CORE_V2_ENABLED:-true}
      AUTOMATION_WAHA_INTERNAL_URL: ${AUTOMATION_WAHA_INTERNAL_URL:-http://waha:3000}
      # Sessao fisica unica da WAHA Core ("default", 1 numero). "@channel" = modo
      # por-conta, valido so com WAHA Plus (multi-sessao).
      AUTOMATION_WAHA_SESSION: ${AUTOMATION_WAHA_SESSION:-default}
      AUTOMATION_N8N_INTERNAL_URL: ${AUTOMATION_N8N_INTERNAL_URL:-http://n8n:5678}
      AUTOMATION_RUNTIME_TOKEN: ${AUTOMATION_RUNTIME_TOKEN}
      # Calendario: plano mensal, chat e transcricao executados pelo n8n interno.
      # As chaves dos providers continuam no banco e viajam somente no payload server-to-server.
      CALENDAR_AI_WEBHOOK_URL: ${CALENDAR_AI_WEBHOOK_URL:-}
      CALENDAR_AI_SERVICE_TOKEN: ${CALENDAR_AI_SERVICE_TOKEN:-}
      CALENDAR_AI_CALLBACK_BASE: ${CALENDAR_AI_CALLBACK_BASE:-http://api:8080}
      CALENDAR_CHAT_WEBHOOK_URL: ${CALENDAR_CHAT_WEBHOOK_URL:-}
      CALENDAR_TRANSCRIBE_WEBHOOK_URL: ${CALENDAR_TRANSCRIBE_WEBHOOK_URL:-}
      # AES-256-GCM para credenciais do omnichannel em repouso. Obrigatoria e sem
      # default: perder/trocar a chave invalida os ciphertexts ja persistidos.
      OMNI_SECRETS_KEY: ${OMNI_SECRETS_KEY:-}
      # Storage privado do inbox. Nao compartilhar com /uploads (servido publicamente).
      OMNICHANNEL_MEDIA_DIR: ${OMNICHANNEL_MEDIA_DIR:-/app/data/media/omnichannel}
      OMNI_AI_EXECUTOR: ${OMNI_AI_EXECUTOR:-native}
      OMNI_N8N_BRAIN_WEBHOOK_URL: ${OMNI_N8N_BRAIN_WEBHOOK_URL:-}
      OMNI_N8N_MEDIA_WEBHOOK_URL: ${OMNI_N8N_MEDIA_WEBHOOK_URL:-}
      OMNI_N8N_INTERNAL_TOKEN: ${OMNI_N8N_INTERNAL_TOKEN:-}
      OMNI_INTERNAL_API_BASE_URL: ${OMNI_INTERNAL_API_BASE_URL:-http://api:8080}
      # Evolution do atendimento omnichannel. A credencial por instancia cifrada
      # no PostgreSQL vence este fallback de ambiente.
      EVOLUTION_BASE_URL: ${EVOLUTION_BASE_URL:-http://evolution:8080}
      EVOLUTION_API_KEY: ${EVOLUTION_API_KEY:-}
      OMNI_DEFAULT_WHATSAPP_PROVIDER: ${OMNI_DEFAULT_WHATSAPP_PROVIDER:-evolution}
      WEBHOOK_RECEIVER_BASE_URL: ${WEBHOOK_RECEIVER_BASE_URL:-https://omni.crowvisuals.com.br}
      WEB_APP_URL: ${WEB_APP_URL}
      UPLOADS_DIR: /app/data/uploads
      # Absolutiza /uploads/* nas respostas publicas (cardapio/bio). Sem isso o
      # publico devolve caminho relativo e o front em outro dominio (ex.: TAVOLA
      # em mostarda.crowvisuals.com.br) nao acha a imagem. Ver DEPLOY_MOSTARDA_PROD.md.
      PUBLIC_API_BASE_URL: ${PUBLIC_API_BASE_URL:-}
      # Base absoluta dos links curtos/QR do modulo tools (shortUrl = {base}/s/{slug}).
      # Vazio = usa PUBLIC_API_BASE_URL. Prod: crowvisuals.com.br (ver PLANO_MODULO_TOOLS.md).
      TOOLS_PUBLIC_BASE_URL: ${TOOLS_PUBLIC_BASE_URL:-}
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      # AC-04: role de runtime least-privilege. Criar a role ANTES do deploy
      # (runbook: docs/MULTITENANT_COMPLETION_PLAN.md, Notas de Deploy AC-04).
      DATABASE_APP_URL: postgres://${APP_DB_ROLE:-omni_app}:${APP_DB_ROLE_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      DATABASE_MIN_CONNS: ${DATABASE_MIN_CONNS:-0}
      DATABASE_MAX_CONNS: ${DATABASE_MAX_CONNS:-10}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      AUTH_TOKEN_SECRET: ${AUTH_TOKEN_SECRET}
      AUTH_TOKEN_TTL: ${AUTH_TOKEN_TTL:-12h}
      AUTH_INVITE_TTL: ${AUTH_INVITE_TTL:-168h}
      AUTH_PASSWORD_RESET_TTL: ${AUTH_PASSWORD_RESET_TTL:-30m}
      # Cookie de sessao do gate SSO (Caddy forward_auth -> /v1/auth/gateway/verify).
      # Prod: .crowvisuals.com.br (cookie vale em omni./n8n./waha.). Ver SSO_GATEWAY_PLAN.md.
      AUTH_GATEWAY_COOKIE_DOMAIN: ${AUTH_GATEWAY_COOKIE_DOMAIN:-}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL:-}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME:-Omni}
      SMTP_TLS_MODE: ${SMTP_TLS_MODE:-starttls}
      SMTP_INSECURE_SKIP_VERIFY: ${SMTP_INSECURE_SKIP_VERIFY:-false}
      SMTP_TIMEOUT: ${SMTP_TIMEOUT:-10s}
      AUTH_BCRYPT_COST: ${AUTH_BCRYPT_COST:-10}
      AUTH_CONSULTANT_EMAIL_DOMAIN: ${AUTH_CONSULTANT_EMAIL_DOMAIN}
      AUTH_CONSULTANT_DEFAULT_PASSWORD: ${AUTH_CONSULTANT_DEFAULT_PASSWORD}
      ERP_SOURCE_KIND: ${ERP_SOURCE_KIND:-local}
      ERP_SOURCE_RECURSIVE: ${ERP_SOURCE_RECURSIVE:-false}
      ERP_SOURCE_DIR: ${ERP_SOURCE_DIR:-/app/data/erp/source}
      ERP_STORAGE_DIR: ${ERP_STORAGE_DIR:-/app/data/erp/storage}
      ERP_BOOTSTRAP_ITEM_FILE: ${ERP_BOOTSTRAP_ITEM_FILE:-consolidados/184/item_184_consolidado.md}
      ERP_BOOTSTRAP_CUSTOMER_FILE: ${ERP_BOOTSTRAP_CUSTOMER_FILE:-consolidados/184/customer_184_consolidado.md}
      ERP_BOOTSTRAP_EMPLOYEE_FILE: ${ERP_BOOTSTRAP_EMPLOYEE_FILE:-consolidados/184/employee_184_consolidado.md}
      ERP_BOOTSTRAP_ORDER_FILE: ${ERP_BOOTSTRAP_ORDER_FILE:-consolidados/184/order_184_consolidado.md}
      ERP_BOOTSTRAP_ORDER_CANCELED_FILE: ${ERP_BOOTSTRAP_ORDER_CANCELED_FILE:-consolidados/184/ordercanceled_184_consolidado.md}
      ERP_BOOTSTRAP_TENANT_ID: ${ERP_BOOTSTRAP_TENANT_ID:-}
      ERP_BOOTSTRAP_TENANT_SLUG: ${ERP_BOOTSTRAP_TENANT_SLUG:-}
      ERP_BOOTSTRAP_STORE_CODE: ${ERP_BOOTSTRAP_STORE_CODE:-184}
      ERP_BOOTSTRAP_STORE_NAME: ${ERP_BOOTSTRAP_STORE_NAME:-Loja 184}
      ERP_BOOTSTRAP_STORE_CITY: ${ERP_BOOTSTRAP_STORE_CITY:-}
      ERP_FTP_HOST: ${ERP_FTP_HOST:-}
      ERP_FTP_PORT: ${ERP_FTP_PORT:-0}
      ERP_FTP_USER: ${ERP_FTP_USER:-}
      ERP_FTP_PASSWORD: ${ERP_FTP_PASSWORD:-}
      ERP_FTP_KEY_PATH: ${ERP_FTP_KEY_PATH:-}
      ERP_FTP_REMOTE_DIR: ${ERP_FTP_REMOTE_DIR:-}
      ERP_FTP_HOST_KEY: ${ERP_FTP_HOST_KEY:-}
      ERP_ROOT_STORE_CODE: ${ERP_ROOT_STORE_CODE:-184}
      ERP_ALLOW_MANUAL_SYNC: ${ERP_ALLOW_MANUAL_SYNC:-false}
      ERP_SYNC_INTERVAL: ${ERP_SYNC_INTERVAL:-24h}
      ERP_SYNC_HOUR_UTC: ${ERP_SYNC_HOUR_UTC:-4}
      ERP_SYNC_AUTOMATIC_ENABLED: ${ERP_SYNC_AUTOMATIC_ENABLED:-false}
      ERP_SYNC_DRY_RUN_DEFAULT: ${ERP_SYNC_DRY_RUN_DEFAULT:-false}
      ERP_CSV_MAX_BYTES: ${ERP_CSV_MAX_BYTES:-134217728}
      ERP_MANUAL_SYNC_MAX_FILES: ${ERP_MANUAL_SYNC_MAX_FILES:-100}
      ERP_BACKFILL_MAX_FILES: ${ERP_BACKFILL_MAX_FILES:-1000}
      ERP_MANUAL_SYNC_MIN_INTERVAL: ${ERP_MANUAL_SYNC_MIN_INTERVAL:-5m}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:${API_PORT:-18080}:8080"
    volumes:
      - api_uploads:/app/data/uploads
      - api_omnichannel_media:/app/data/media/omnichannel
      - ${ERP_SOURCE_HOST_DIR:-./erp-source}:/app/data/erp/source:ro
      - api_erp_storage:/app/data/erp/storage
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl --fail --silent http://127.0.0.1:8080/healthz > /dev/null || exit 1"
        ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks:
      app:
      proxy:
        aliases:
          - ${PROXY_API_ALIAS:-lista-api}

  web:
    # Imagem do registry (GHCR). Mesmo IMAGE_TAG (SHA) que a api: prod e staging
    # promovem o MESMO artefato. `build:` abaixo so pra build local de emergencia/dev.
    image: ${WEB_IMAGE:-ghcr.io/mikewade2k16/omni-web}:${IMAGE_TAG:-latest}
    build:
      context: ./web
      dockerfile: Dockerfile
    restart: unless-stopped
    mem_limit: 768m
    mem_reservation: 128m
    cpus: 1.0
    logging: *default-logging
    environment:
      NODE_ENV: production
      NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE}
      NUXT_PUBLIC_API_WS_BASE: ${NUXT_PUBLIC_API_WS_BASE:-}
      # Base do Studio do TAVOLA (repo separado) embutido por iframe na aba "Site" do
      # editor de cardápio (site builder). Sem isso o painel cai no default localhost:3000
      # e a aba Site não embute. Aponte pro host do TAVOLA (ex.: https://mostarda.crowvisuals.com.br).
      NUXT_PUBLIC_STUDIO_URL: ${NUXT_PUBLIC_STUDIO_URL:-}
      NUXT_API_INTERNAL_BASE: ${NUXT_API_INTERNAL_BASE:-http://api:8080}
      NITRO_HOST: 0.0.0.0
      PORT: 3003
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "127.0.0.1:${WEB_PORT:-13003}:3003"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3003/').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      app:
      proxy:
        aliases:
          - ${PROXY_WEB_ALIAS:-lista-web}

  # ── Automacao WhatsApp/IA (n8n + WAHA + Redis) — profile "automation" ──
  # Sobe so com: docker compose -f docker-compose.prod.yml --profile automation up -d
  # Mesmos nomes de servico do dev (redis/waha/n8n) para o workflow e as credenciais
  # do n8n funcionarem igual local e prod (WAHA->n8n por http://n8n:5678; credencial
  # Redis host "redis"; WAHA host "http://waha:3000"). O Caddy (rede proxy) roteia o
  # editor do n8n e o dashboard da WAHA por subdominio com basic auth; o webhook
  # WAHA->n8n NUNCA sai da rede interna. O Redis fica so na rede "app" (nunca publico)
  # e ja disponivel para a API Go do Omni usar no futuro (redis:6379). Sem postgres
  # dedicado: a integracao com dados usa o Postgres do Omni por fase (PLANO A1+).
  # Runbook de deploy: docs/automation/SETUP.md secao 8.
  redis:
    image: redis:8-alpine
    profiles: ["automation"]
    restart: unless-stopped
    mem_limit: 256m
    mem_reservation: 32m
    cpus: 0.5
    logging: *default-logging
    command: redis-server --requirepass ${AUTOMATION_REDIS_PASSWORD}
    # REDIS_PASSWORD dentro do container so p/ o healthcheck autenticar (mesma var
    # que o command ja usa; nenhuma env nova no .env da VPS).
    environment:
      REDIS_PASSWORD: ${AUTOMATION_REDIS_PASSWORD}
    volumes:
      - automation_redis:/data
    ports:
      - "127.0.0.1:${AUTOMATION_REDIS_PORT:-16380}:6379"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli --no-auth-warning -a \"$$REDIS_PASSWORD\" ping | grep -q PONG"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - app

  waha:
    # 2026.7.1 corrige GOWS silenciosamente parado apos <stream:error> de media ack.
    image: devlikeapro/waha:gows-2026.7.1
    profiles: ["automation"]
    restart: unless-stopped
    # O incidente de 2026-07-22 atingiu exatamente o antigo teto de 1 GiB durante
    # sincronizacao. 2 GiB absorve o pico sem deixar o processo Node vivo e o GOWS morto.
    mem_limit: ${AUTOMATION_WAHA_MEMORY_LIMIT:-2g}
    mem_reservation: 256m
    cpus: 1.5
    logging: *default-logging
    environment:
      WHATSAPP_HOOK_URL: http://n8n:5678/webhook/webhook
      WHATSAPP_DEFAULT_ENGINE: GOWS
      WHATSAPP_HOOK_EVENTS: message
      # Reabre a sessao persistida depois de restart/OOM sem exigir clique ou novo QR.
      WHATSAPP_RESTART_ALL_SESSIONS: "true"
      WAHA_AUTO_START_DELAY_SECONDS: "1"
      # O bot nao consome stories/status; filtrar na origem reduz webhooks e storage.
      WAHA_SESSION_CONFIG_IGNORE_STATUS: ${AUTOMATION_WAHA_IGNORE_STATUS:-true}
      WAHA_NO_API_KEY: "true"
      WAHA_DASHBOARD_USERNAME: ${AUTOMATION_WAHA_DASHBOARD_USER:-admin}
      WAHA_DASHBOARD_PASSWORD: ${AUTOMATION_WAHA_DASHBOARD_PASSWORD}
      WHATSAPP_SWAGGER_NO_PASSWORD: "true"
    volumes:
      - automation_waha_sessions:/app/.sessions
      - automation_waha_media:/app/.media
    ports:
      - "127.0.0.1:${AUTOMATION_WAHA_PORT:-13010}:3000"
    healthcheck:
      # restart_policy so reage quando PID 1 sai. Se o HTTP morrer depois de ja ter
      # ficado pronto (caso real: GOWS parou e o Node ficou vivo), tres falhas encerram
      # PID 1; Docker reinicia e o autostart acima restaura a sessao persistida.
      # O marcador inclui o starttime de PID 1 para nao matar um boot ainda iniciando.
      test:
        - CMD-SHELL
        - >-
          boot=$$(awk '{print $$22}' /proc/1/stat);
          if curl --fail --silent --max-time 5 http://127.0.0.1:3000/ping > /dev/null; then
            echo "$$boot" > /tmp/waha-health-ready;
            rm -f /tmp/waha-health-failures;
            exit 0;
          fi;
          ready=$$(cat /tmp/waha-health-ready 2>/dev/null || true);
          [ "$$ready" = "$$boot" ] || exit 1;
          failures=$$(cat /tmp/waha-health-failures 2>/dev/null || echo 0);
          failures=$$((failures + 1));
          echo "$$failures" > /tmp/waha-health-failures;
          if [ "$$failures" -ge 3 ]; then kill -TERM 1; fi;
          exit 1
      interval: 20s
      timeout: 8s
      retries: 3
      start_period: 90s
    # Ordena so o boot (service_started, nao _healthy): a WAHA precisa subir mesmo
    # se o n8n estiver quebrado, p/ nao travar QR/dashboard.
    depends_on:
      n8n:
        condition: service_started
    networks:
      app:
      proxy:
        aliases:
          - ${AUTOMATION_WAHA_PROXY_ALIAS:-automation-waha}

  n8n:
    image: n8nio/n8n:2.23.2
    profiles: ["automation"]
    restart: unless-stopped
    mem_limit: 768m
    mem_reservation: 192m
    cpus: 1.0
    logging: *default-logging
    environment:
      N8N_HOST: ${AUTOMATION_N8N_HOST}
      N8N_PORT: 5678
      # http/false p/ acesso via tunel SSH (dev/bootstrap); default https/true p/ subdominio.
      N8N_PROTOCOL: ${AUTOMATION_N8N_PROTOCOL:-https}
      WEBHOOK_URL: ${AUTOMATION_N8N_WEBHOOK_URL}
      N8N_EDITOR_BASE_URL: ${AUTOMATION_N8N_WEBHOOK_URL}
      N8N_PROXY_HOPS: 1
      N8N_SECURE_COOKIE: ${AUTOMATION_N8N_SECURE_COOKIE:-true}
      N8N_ENCRYPTION_KEY: ${AUTOMATION_N8N_ENCRYPTION_KEY}
      GENERIC_TIMEZONE: America/Sao_Paulo
      N8N_LOG_LEVEL: info
      N8N_COMMUNITY_PACKAGES_ALLOW_TOOL_USAGE: "true"
      # Libera $env.* nas expressoes do n8n. Os workflows usam $env.AUTOMATION_RUNTIME_TOKEN
      # no header das tools (Omni Chat: "Buscar catalogo") e no runtime-config (WhatsApp).
      # Sem isso o n8n bloqueia e da "access to env vars denied". Espelha o dev.
      N8N_BLOCK_ENV_ACCESS_IN_NODE: "false"
      AUTOMATION_RUNTIME_TOKEN: ${AUTOMATION_RUNTIME_TOKEN}
      # Omnichannel Brain: token interno e endpoint do gateway Go; nenhuma chave de provider.
      OMNI_N8N_INTERNAL_TOKEN: ${OMNI_N8N_INTERNAL_TOKEN:-}
      OMNI_LLM_GATEWAY_URL: ${OMNI_LLM_GATEWAY_URL:-http://api:8080/v1/runtime/omnichannel/llm-gateway}
      OMNI_MEDIA_GATEWAY_URL: ${OMNI_MEDIA_GATEWAY_URL:-http://api:8080/v1/runtime/omnichannel/media}
    volumes:
      - automation_n8n_data:/home/node/.n8n
    ports:
      - "127.0.0.1:${AUTOMATION_N8N_PORT:-15680}:5678"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:5678/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    depends_on:
      redis:
        condition: service_healthy
    networks:
      app:
      proxy:
        aliases:
          - ${AUTOMATION_N8N_PROXY_ALIAS:-automation-n8n}

  # Whisper (transcricao de voz do calendario): o n8n (workflow calendar-transcribe, ramo
  # "local") bate em http://whisper:8000. CPU, modelo base int8, sempre carregado (TTL -1).
  # Profile automation; SO na rede interna `app` (n8n resolve pelo nome do servico) — sem
  # porta exposta no host. mem 2g (o base int8 usa <1g; teto folgado). Espelha o dev.
  whisper:
    image: fedirz/faster-whisper-server:latest-cpu
    profiles: ["automation"]
    restart: unless-stopped
    mem_limit: 2g
    mem_reservation: 256m
    logging: *default-logging
    environment:
      WHISPER__MODEL: ${WHISPER_MODEL:-Systran/faster-whisper-base}
      WHISPER__INFERENCE_DEVICE: cpu
      WHISPER__COMPUTE_TYPE: int8
      WHISPER__TTL: "-1"
      WHISPER__CPU_THREADS: "0"
    volumes:
      - automation_whisper_cache:/root/.cache/huggingface
    networks:
      - app

  # Provider Evolution do atendimento omnichannel. Banco e volume sao exclusivos;
  # nao compartilha estado com o PostgreSQL autoritativo do Omni nem com a WAHA.
  evolution-db:
    image: postgres:16-alpine
    profiles: ["omnichannel"]
    restart: unless-stopped
    mem_limit: 512m
    mem_reservation: 128m
    cpus: 0.75
    logging: *default-logging
    environment:
      POSTGRES_DB: evolution
      POSTGRES_USER: evolution
      POSTGRES_PASSWORD: ${EVOLUTION_DB_PASSWORD}
    volumes:
      - evolution_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U evolution -d evolution"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - app

  evolution:
    image: evoapicloud/evolution-api:v2.3.7
    profiles: ["omnichannel"]
    restart: unless-stopped
    mem_limit: 1g
    mem_reservation: 256m
    cpus: 1.5
    logging: *default-logging
    environment:
      SERVER_TYPE: http
      SERVER_PORT: "8080"
      SERVER_URL: ${EVOLUTION_BASE_URL:-http://evolution:8080}
      CONFIG_SESSION_PHONE_VERSION: ${EVOLUTION_CONFIG_SESSION_PHONE_VERSION:-2,3000,1025205472}
      AUTHENTICATION_TYPE: apikey
      AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY}
      DATABASE_ENABLED: "true"
      DATABASE_PROVIDER: postgresql
      DATABASE_CONNECTION_URI: postgresql://evolution:${EVOLUTION_DB_PASSWORD}@evolution-db:5432/evolution?schema=public
      DATABASE_CONNECTION_CLIENT_NAME: evolution_omni
      DATABASE_SAVE_DATA_INSTANCE: "true"
      DATABASE_SAVE_DATA_NEW_MESSAGE: "true"
      DATABASE_SAVE_MESSAGE_UPDATE: "true"
      DATABASE_SAVE_DATA_CONTACTS: "true"
      DATABASE_SAVE_DATA_CHATS: "true"
      CACHE_REDIS_ENABLED: "false"
      CACHE_LOCAL_ENABLED: "true"
      WEBHOOK_GLOBAL_ENABLED: "false"
      LOG_LEVEL: ${EVOLUTION_LOG_LEVEL:-ERROR}
      LOG_BAILEYS: error
    volumes:
      - evolution_instances:/evolution/instances
    ports:
      - "127.0.0.1:${EVOLUTION_PORT:-18085}:8080"
    depends_on:
      evolution-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:8080/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - app

volumes:
  postgres_data:
  api_uploads:
  api_omnichannel_media:
  api_erp_storage:
  automation_redis:
  automation_waha_sessions:
  automation_waha_media:
  automation_n8n_data:
  automation_whisper_cache:
  evolution_db_data:
  evolution_instances:

networks:
  app:
  proxy:
    external: true
    name: ${PROXY_NETWORK_NAME:-omnichannel-mvp_default}
