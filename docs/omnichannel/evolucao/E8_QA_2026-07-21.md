# E8 — QA local e ponto de pausa (2026-07-21)

## Resultado

E8 está `CODE-COMPLETE` localmente. O adapter Instagram, ingestão de DM/comentários, CRM/inbox
único, moderação e painel foram ligados sem alterar WAHA, Evolution, Calendar, Operation ou
workflows n8n fora do ownership do Omnichannel.

## Evidências executadas

- `go test ./internal/modules/omnichannel/...` — verde, incluindo parser/HMAC/send Instagram;
- `go vet ./internal/modules/omnichannel/...` — verde;
- `npm test -- --run` — 27 arquivos / 158 testes verdes;
- ESLint dos arquivos FE da fase — sem erros (há apenas warning histórico de max-lines em
  `config-api.ts`);
- `vue-tsc --noEmit` — somente erros preexistentes fora do pacote Omnichannel (admin/calendar e
  tipos legados); nenhum erro nos arquivos desta fase;
- guards n8n — `89 testes` verdes;
- normalizador dos dois workflows do Omnichannel — `STATUS:unchanged`;
- migration `0226`/`0227` aplicada no PostgreSQL pelo boot do `api` (`migration_up_ok`);
- `GET http://localhost:9091/healthz` e `GET http://localhost:3003/` — HTTP 200;
- somente `api` e `web` foram recriados. `evolution`, `evolution-db`, `n8n`, `postgres` e `redis`
  não foram reiniciados.

## Guardas de segurança

- tokens/app secret/verify token ficam cifrados; respostas do painel são mascaradas;
- webhook Meta valida HMAC/challenge e resolve `phone_number_id`/`ig_user_id` dentro da conta;
- toda ação de comentário é account-scoped, idempotente e sai pela outbox/worker Go;
- IA em comentário/menção grava apenas `proposed_text`; não chama `SendAIMessage` nem publica;
- `workflow-instagram-first-contact.json` permanece inativo, sem credencial/PII de runtime e sem
  chamada direta à Graph API;
- `workflow-whatsapp.json`, WAHA e todos os workflows de outros módulos permanecem fora de escopo.

## Ponto exato para retomada

Pausar aqui conforme orientação do dono. A próxima fase é E9 (hardening/observabilidade/escala),
seguida de E10 (rollout). O que falta para colocar E7/E8 em produção é somente configurar credenciais
Meta reais, validar App Review/capabilities, executar smoke controlado e fazer cutover/rollback por
número/conta — sem ativar workflows ou alterar os módulos protegidos.
