const crypto = require('crypto'); const secret = 'dev-secret-change-me'; const now = Math.floor(Date.now() / 1000); const payload = { sub: 'cccccccc-cccc-cccc-cccc-ccccccccc005', email: 'mikewade2k16@gmail.com', role: 'platform_admin', iat: now - 30, exp: now + 3600 }; const payloadB64 = Buffer.from(JSON.stringify(payload)).toString('base64url'); const signature = crypto.createHmac('sha256', secret).update(payloadB64).digest('base64url'); const token = 'ldv1.' + payloadB64 + '.' + signature; console.log(token);
