{
  "name": "lista-da-vez",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "dev:3001": "npm --prefix web run dev:3001",
    "build": "npm --prefix web run build",
    "generate": "npm --prefix web run generate",
    "preview": "npm --prefix web run preview"
  }
}
