import type { RoadmapPhase } from "./types";

export const ROADMAP_PHASES_PART6: RoadmapPhase[] = [
  // ─── Pré-venda / Pré-deploy — revisão tripla 2026-06-29 ───────────────────
  //
  // Criada em 2026-06-29 a partir da revisão tripla adversarial (9 achados de
  // segurança + 36 de faxina). Reúne o que precisa estar fechado ANTES de vender
  // acesso a cliente / antes do deploy: staging do trabalho não-commitado +
  // env vars novas na VPS, gate RBAC do cardápio + CheckMembership do assignRole
  // (wave 1), smokes no browser dos critérios 1-3 do MULTITENANT_COMPLETION_PLAN,
  // e a wave 2 de faxina (refactors grandes). Docs canônicos:
  // docs/SECURITY_OPTIMIZATION_BACKLOG.md (§Revisão 2026-06-29, SEC-6..SEC-14) e
  // docs/FAXINA_CODIGO_2026-06-29.md.
  {
    id: "pre-venda-revisao-0629",
    code: "PRE",
    title: "Pré-venda / Pré-deploy — fechar revisão tripla 2026-06-29",
    goal: "Deixar o painel pronto para vender acesso e fazer deploy: tudo da revisão tripla de 2026-06-29 staged/commitado + env vars novas provisionadas na VPS, gate RBAC do cardápio e CheckMembership do assignRole validados no browser (wave 1), smokes dos critérios 1-3 do MULTITENANT_COMPLETION_PLAN e a wave 2 de faxina (refactors grandes) agendada. Segurança: docs/SECURITY_OPTIMIZATION_BACKLOG.md §Revisão 2026-06-29; faxina: docs/FAXINA_CODIGO_2026-06-29.md.",
    status: "in_progress",
    startedAt: "2026-06-29",
    estimateWeeks: "wave 1 + wave 2 commitadas; faltam smokes no browser + deferidos (split useTasksPageContext, slugify) + pré-existentes",
    group: "multi-tenant",
    tasks: [
      // (a) Deploy — staging do trabalho + env vars na VPS
      { id: "pre-a-git-add", label: "[deploy] git add -A obrigatório: o trabalho da revisão (cardápio/rbac/faxina) tem arquivos novos e alterados ainda não commitados; stage tudo (sem deixar arquivo novo de fora) antes de buildar a imagem do deploy", done: false, note: "Achado da revisão tripla 2026-06-29: dezenas de arquivos novos/alterados untracked na branch; um `git add -A` antes do build evita imagem velha (lição do deploy-ship órfão). Comandos devolvidos ao usuário (regra: agente não roda git)." },
      { id: "pre-a-env-vps", label: "[deploy] Provisionar no .env.production da VPS: CARDAPIO_TELEMETRY_SALT (obrigatório p/ ip_hash da telemetria; hoje só loga Warn e segue com hash vazio) e CARDAPIO_TELEMETRY_RETENTION_DAYS (opcional, default 90)", done: false, note: "SEC-13: module.go:72-83 só Warn quando o salt é vazio; doc dizia 'obrigatório'. Alinhar = provisionar em prod. Anotar em Notas de Deploy do doc canônico." },
      // (b) Segurança wave 1 — gate RBAC cardápio + CheckMembership assignRole
      { id: "pre-b-cardapio-rbac", label: "[seguranca] Gate RBAC fino do cardápio (wave 1): /v1/cardapio/* exige cardapio.view (GET) e cardapio.manage (escrita); workspace cardapio_web ganha viewPermission/editPermission; validar no browser que papel sem permissão não vê/edita e admin/agência continua", done: false, note: "SEC-6 (back/internal/modules/cardapio/http.go:17-39, era RequireAuth-only) + SEC-8 (web/app/utils/workspaces.ts:34-39, sem chaves de permissão) + SEC-14 (AGENT.md afirmava RBAC que não existia). platform_admin/agency_owner em curto-circuito. Validar no browser." },
      { id: "pre-b-assignrole-membership", label: "[seguranca] CheckMembership no assignRole (wave 1): AssignRoleToUser valida que o alvo é membro da account antes de conceder o papel (não-membro/cross-tenant → 404); validar no browser que não dá pra atribuir papel a usuário de outro tenant", done: false, note: "SEC-7 (back/internal/modules/core/rbac_service.go:179-193): CheckMembership + ErrAccountNotMember→ErrNotMember→404, espelhando SetUserRoles. Exige rebuild api." },
      { id: "pre-b-feedback-404-tenant", label: "[seguranca] Feedback: 403→404 fora de escopo (SEC-9) + predicado tenant_id nas queries (SEC-10, defesa em profundidade) — wave 1; validar IDOR cross-tenant no browser", done: false, note: "SEC-9 (feedback/http.go:355-356) + SEC-10 (feedback/store_postgres.go:138,157,217). ErrForbidden por escopo vira 404; por permissão RBAC continua 403." },
      { id: "pre-b-cardapio-ratelimit-ip", label: "[seguranca] Cardápio público: rate-limit por restaurante+IP (SEC-11) + clientIP confiável (SEC-12, não confiar no 1º X-Forwarded-For) — wave 1", done: false, note: "SEC-11 (rate_limit.go:36-40, chave global por IP→incluir restaurante) + SEC-12 (rate_limit.go:65-77, pegar IP à direita do XFF ou RemoteAddr). NÃO remover o limitador público (cobre rotas sem JWT, de propósito)." },
      // (c) Smokes no browser — critérios 1-3 do MULTITENANT_COMPLETION_PLAN
      { id: "pre-c-smoke-crit1", label: "[smoke] Critério 1: GET /v1/admin/accounts retorna as contas reais do banco (não mock) — conferir no browser logado", done: false, note: "Espelha o critério 1 do verifiable da fase multitenant-completion. Validação humana no browser." },
      { id: "pre-c-smoke-crit2", label: "[smoke] Critério 2: habilitar módulo via UI grava em core.account_modules e o item aparece no menu sem reload", done: false },
      { id: "pre-c-smoke-crit3", label: "[smoke] Critério 3: desabilitar módulo retorna 403 module_disabled na rota dele (gate por RequireModuleByPath)", done: false },
      // (d) Wave 2 — faxina (refactors grandes)
      { id: "pre-d-wave2-helpers", label: "[wave2] Consolidar helpers cross-módulo (firstNonEmpty 8x, normalizeStoreIDs cross-módulo, decodeStringSlice 3x, slugify 6x) num pacote utilitário único + testes", done: false, note: "FEITO wave 2 (commitado): pacote back/internal/platform/stringsx (FirstNonEmpty/NormalizeIDs/DecodeJSONStringSlice) + teste; ~18 call-sites migrados. PENDENTE: slugify (F-04) — 6 variantes geram slugs diferentes (NFD × sem-acento × só-lowercase); unificar muda o slug = decisão de produto." },
      { id: "pre-d-wave2-splits", label: "[wave2] Split de arquivos > 450 linhas: useTasksPageContext.ts (3063), AdminUsersWorkspace.vue (881), AdminRoleMatrixEditor.vue (633), admin_users_repository.go (592)", done: false, note: "FEITO wave 2: AdminUsersWorkspace.vue 881→491 (F-18), AdminRoleMatrixEditor.vue 633→492 (F-19), admin_users_repository.go 592→~420 (F-20) + struct RoleTemplate removida. PENDENTE: split do useTasksPageContext.ts 3063 (F-17) — adiado, fazer incremental com validação no browser." },
      { id: "pre-d-wave2-socket-inline-chat", label: "[wave2] Extrair socket WS compartilhado (useTasksRealtime/useTaskPresence), useInlineEditManager (6 managers) e composable de chat do feedback", done: true, note: "FEITO wave 2 (commitado): useRealtimeSocket.ts (socket WS compartilhado) + useInlineEditManager.ts (6 managers via inject) + useFeedbackChat.js (chat admin+user). Refactor puro, gates verdes (go build/vet/test + eslint 0 erros + vue-tsc sem erro novo). Falta smoke de realtime/inline-edit no browser." },
      // (e) Wave 3 — arrumar PRE-EXISTENTES (em andamento 2026-06-29). Modelos: opus high nas 2 difíceis, sonnet high no resto.
      { id: "pre-e-w3-vuetsc", label: "[wave3] Deixar o vue-tsc da baseline verde (sem any/@ts-ignore): tipar os {}/unknown em api-client.ts + runtime-remote* (opus), corrigir sourceValue/cast/typo prioritySet nos composables de tasks, e o tipo da prop empty/caption em OmniDataTable/TasksBoardView", done: false, note: "Wave 3: alvos corrigidos (api-client ApiRequestMethod, runtime-remote tipados, sourceValue/casts nos composables, prop creatable em OmniSelect destravando OmniDataTable/TasksBoardView), sem any/@ts-ignore. Total vue-tsc 348→340. PARCIAL: a baseline AINDA tem ~340 erros pré-existentes (apiRequest sem genérico->unknown, AppDatePicker date-lib) — 'vue-tsc 100% verde' é frente dedicada (centenas de erros). ADIADO a pedido (2026-06-29): avaliar SE vale e O QUE acarreta antes de mexer nos ~340 erros pré-existentes — NÃO tocar sem decisão (risco/retorno ainda incerto)." },
      { id: "pre-e-w3-n1", label: "[wave3] Eliminar o N+1 do MeAccounts/MeContext: batch ListEnabledModuleIDsForAccounts (WHERE account_id = ANY($1)) em vez de 1 query por conta — OPT-2/F-22", done: true, note: "FEITO wave 3 (revisão opus + build da imagem verde/healthy): batch ListEnabledModuleIDsForAccounts (WHERE account_id = ANY($1::uuid[])) no MeAccounts; MeContext (N=1) segue no singular; fallback p/ fakes de teste preservado. Validar no browser: switcher de contas com os módulos certos por conta." },
      { id: "pre-e-w3-compose", label: "[wave3] Remover o AUTH_ROLES_SOURCE morto dos docker-compose + .env*.example + marcar no LEGADO (F-08)", done: true, note: "FEITO wave 3: AUTH_ROLES_SOURCE removido dos docker-compose + .env*.example; auth/AGENT.md (linha ~80) e docs/LEGADO.md atualizados. Go não lia a flag em runtime desde a wave 1." },
      // (f) Frentes pesadas pós-revisão (em andamento 2026-06-29) — decididas com o usuário.
      { id: "pre-f-rls", label: "[seguranca · FUTURO/PARADO] RLS no Postgres (SEC-1): 2ª rede de isolamento multi-tenant (defesa em profundidade)", done: false, note: "PARADO p/ outro momento (decisão 2026-06-29). O QUE É: Row-Level Security, recurso nativo do Postgres — uma regra (policy) no banco que filtra automaticamente QUAIS LINHAS cada query enxerga, mesmo sem WHERE. POR QUÊ: hoje o isolamento entre clientes é 100% no código (filtro tenant_id no app, que FUNCIONA); o RLS seria a 2ª rede — se um dia uma query nova esquecer o filtro, o próprio BANCO barra o vazamento. STATUS: só IDEIA. A tentativa de fundação (wave 4) foi REMOVIDA do código em 2026-06-29 (era inerte sob superuser + a migration quebrava o boot; o código fica no histórico em cabf5b2 se precisar). A proteção real continua sendo o filtro de tenant no app. Quando refazer, será no SISTEMA INTEIRO, controlado. 2 BLOQUEIOS pra virar real: (1) a app conecta como omni=SUPERUSER, que o Postgres faz IGNORAR RLS — precisa de um role de app comum (não-superuser, sem BYPASSRLS) + GRANTs + DATABASE_URL; (2) os public.* viraram VIEWS (RLS não vai em view) — migration schema-qualificada nas tabelas base + security_invoker. PERFORMANCE (a AVALIAR antes de ligar): +1 predicado por query (pequeno COM índice em tenant_id, que a maioria das tabelas já tem, mas medir num endpoint de lista grande) + conn-por-request. Design completo: docs/RLS_PLAN.md." },
      { id: "pre-f-slugify", label: "[faxina] slugify canônico (F-04): unificar as 6 variantes numa regra única (NFD/sem-acento), back (Go) + front (TS) com o MESMO output", done: true, note: "2026-06-29: stringsx.Slugify (Go, NFD) + domain/utils/slugify.ts (TS, p{M}). bio/cardapio trocaram cópias locais; site.perolaSlug é outra lógica (crow-notion, usa '_') e não foi tocada. Slugs gravados no banco não são re-gerados." },
      { id: "pre-f-tasks-split-lazy", label: "[faxina/perf] Split do useTasksPageContext.ts (3063, F-17) + lazy-mount dos cards (F-24) — mesma área web/tasks", done: false, note: "Refactor de reatividade ALTO risco: incremental, preservando return/efeitos; EXIGE smoke no browser (board). Lazy-mount = montar editores pesados só no clique." },
      { id: "pre-f-projlean", label: "[perf] Projeção lean (OPT-4/F-26): listagens devolvem só os campos que a tela usa", done: false, note: "BEHAVIOR-AFFECTING (muda o shape da resposta) — conservador: só cortar campo PROVADAMENTE não usado pelo front, por endpoint; campo incerto fica. Senão quebra a tela silenciosamente." },
    ],
    blockers: [],
    verifiable: "1) git status sem arquivo novo da revisão fora do stage e CARDAPIO_TELEMETRY_SALT/RETENTION_DAYS no .env.production da VPS. 2) No browser: papel sem cardapio.view não vê/edita o cardápio (admin/agência sim) e assignRole não atribui papel cross-tenant (404). 3) Smokes 1-3 do MULTITENANT_COMPLETION_PLAN passam no browser. 4) Wave 2 de faxina agendada/iniciada (helpers consolidados, splits dos arquivos > 450 linhas). Refs: docs/SECURITY_OPTIMIZATION_BACKLOG.md §Revisão 2026-06-29 + docs/FAXINA_CODIGO_2026-06-29.md."
  },

  // ─── Criação de usuário & convite (onboarding / login) ──────────────────
  // Reporte do dono 2026-06-30 ao criar usuário em /manage/users (modal "Novo
  // usuário"). Polimento de UX em cima da UAF (que já fez criar-com-senha-que-loga).
  // Modal: web/app/components/admin/users/AdminUserCreateDialog.vue;
  // back: back/internal/modules/core/admin_users_service.go (validação + BuildNickname).
  {
    id: "user-onboarding",
    code: "UONB",
    title: "Criação de usuário — erro em PT por campo, sugestão de nome/nick e convite por e-mail",
    goal: "Deixar a criação de usuário em /manage/users clara e completa: (1) mensagem de erro em português e POR CAMPO (nunca o termo técnico 'displayName'; e-mail preenchido não pode acusar e-mail faltando); (2) sugerir Nome e Nick a partir do e-mail na modal (editáveis, opcionais, sem sobrescrever o que o usuário digitou); (3) fluxo REAL de convite — quando a senha temporária fica vazia, enviar e-mail para a própria pessoa criar a senha (hoje o texto promete 'convite' mas não há envio). Modal de criação e drawer de edição espelhados.",
    status: "pending",
    estimateWeeks: "2-4 dias",
    startedAt: "2026-06-30",
    tasks: [
      { id: "uonb-erro-pt-campo", label: "Erro em PT por campo: back (admin_users_service.go:87) troca o texto fixo 'email and displayName are required' por erros específicos por campo (e-mail vs nome) num formato que o front mapeia; front (AdminUserCreateDialog) valida antes do submit e mostra o aviso INLINE no campo certo, em português ('Informe o nome.'), nunca 'displayName'. E-mail preenchido NÃO dispara erro de e-mail. Espelha a regra de feedback de formulário (marcar obrigatório/opcional + dizer o que falta).", done: false },
      { id: "uonb-sugestao-nome-nick", label: "Sugestão na modal: ao digitar/sair do e-mail, sugerir Nome (parte antes do @, normalizada/capitalizada) e Nick; ao alterar o Nome, re-sugerir o Nick. Ambos editáveis e OPCIONAIS — só preenche enquanto o campo estiver vazio ou ainda for a sugestão (não sobrescreve o que o usuário digitou à mão). Reusa/espelha BuildNickname (core/nick.go ↔ person-display.ts) p/ o nick bater com o que o back gera quando vazio.", done: false },
      { id: "uonb-convite-email", label: "Convite por e-mail REAL: senha temporária vazia gera token de convite (com expiração) e dispara um e-mail para a pessoa criar a própria senha (página /set-password por token). Endpoints: criar-convite + definir-senha-por-token + reenviar/expirar. UI da modal deixa explícito 'sem senha = enviaremos um convite por e-mail'. REUSA a infra SMTP da fase PWD (auth/password_reset_delivery.go já implementa o envio; o convite só precisa usar o mesmo caminho/credenciais). DECISÃO PENDENTE: provedor/credenciais SMTP (compartilhado com PWD) — anotar nas Notas de Deploy. Depende de pwd-smtp-config.", done: false },
      { id: "uonb-modal-drawer-mirror", label: "Espelhar a UX (validação por campo + sugestão de nome/nick) entre a modal de criação (AdminUserCreateDialog) e o drawer de edição (AdminUserEditDrawer/AdminUserDataPanel) — regra modal↔card: mudança em um replica no outro.", done: false },
      { id: "uonb-docs", label: "Ao concluir, sincronizar AGENT.md (core, components/admin/users) + doc canônico + este roadmap (regra dos 3 docs).", done: false },
    ],
    blockers: [],
    verifiable: "Em /manage/users, criar usuário só com e-mail → o aviso é em português e aponta SÓ o Nome (não 'displayName', não acusa e-mail). Digitar o e-mail preenche Nome e Nick sugeridos (editáveis); mudar o Nome re-sugere o Nick; ambos podem ficar como o usuário quiser. Criar sem senha → a pessoa recebe um e-mail e define a própria senha por um link, e consegue logar. Mesma validação/sugestão no drawer de edição. vue-tsc/eslint/golangci-lint limpos."
  },

  // ─── Esqueci a senha (recuperação) + e-mail transacional (SMTP) ─────────
  // Reporte do dono 2026-06-30: o botão "esqueci a senha" do login existe mas
  // "não serve de nada". DIAGNÓSTICO no código: o fluxo está TODO construído —
  // front web/app/pages/auth/esqueceu-senha.vue (pedir código → confirmar código
  // + nova senha → sucesso) e back POST /v1/auth/password-reset/{request,confirm}
  // (PasswordResetService: código 6 dígitos hasheado, TTL 30min, ConsumePasswordReset).
  // A entrega SMTP TAMBÉM já existe (auth/password_reset_delivery.go: STARTTLS/TLS/none),
  // MAS cai no fallback LogPasswordResetDelivery quando SMTP_HOST está vazio → o
  // código só vai pro LOG, nenhum e-mail sai. Esse é o motivo do "não funciona".
  // Esta mesma infra SMTP destrava o convite por e-mail da fase UONB.
  {
    id: "password-reset",
    code: "PWD",
    title: "Esqueci a senha — ligar o e-mail real (SMTP) + revisar checagens/UX",
    goal: "Fazer a recuperação de senha funcionar de ponta a ponta: configurar o provedor de e-mail (SMTP) para o código realmente chegar (hoje só é logado), revisar/confirmar as checagens (existe/ativo/permissão) e deixar a UX honesta sem virar enumeração de contas. A mesma infra de e-mail destrava o convite da fase UONB.",
    status: "pending",
    estimateWeeks: "1-3 dias (back já existe; o grosso é configurar SMTP + validar)",
    startedAt: "2026-06-30",
    tasks: [
      { id: "pwd-smtp-config", label: "Configurar o provedor de e-mail (SMTP) — o código de entrega JÁ existe (auth/password_reset_delivery.go); falta só ligar: env SMTP_HOST/SMTP_PORT/SMTP_USERNAME/SMTP_PASSWORD/SMTP_FROM_EMAIL/SMTP_FROM_NAME/SMTP_TLS_MODE em dev e prod. Sem SMTP_HOST cai no LogPasswordResetDelivery (só loga o código, não envia). Escolher provedor (SES/Resend/Gmail SMTP/host próprio) + registrar nas Notas de Deploy. ESTE é o motivo de o botão 'não servir de nada'.", done: false },
      { id: "pwd-checagens", label: "Revisar/confirmar as checagens (corrige a percepção 'não checa'): hoje o Request IGNORA em silêncio (retorna sucesso) quando o usuário não existe / está inativo / sem senha — anti-enumeração DE PROPÓSITO; o Confirm checa ativo (ErrUserInactive) e onboarding (ErrOnboardingRequired). Decidir se algum estado extra (ex.: usuário sem nenhum papel/vínculo) deve bloquear o reset e documentar a regra.", done: false },
      { id: "pwd-ux-honesta", label: "UX honesta sem enumeração: a tela sempre avança pra 'digite o código' mesmo p/ e-mail inexistente (por segurança) — deixar isso explícito na UI ('se existir uma conta com este e-mail, enviaremos um código') p/ não parecer bug. Decisão do dono: manter anti-enumeração (recomendado) vs feedback explícito.", done: false },
      { id: "pwd-rate-limit", label: "Anti-brute-force: garantir rate limit no request (cooldown de reenvio) e limite de tentativas no confirm do código de 6 dígitos (lockout). Auditar se já existe; senão adicionar.", done: false },
      { id: "pwd-e2e-docs", label: "Teste e2e real (e-mail chega, código válido redefine a senha, login aceita a nova senha) + sincronizar AGENT.md (auth) + doc canônico + roadmap.", done: false },
    ],
    blockers: [],
    verifiable: "Com SMTP configurado: pedir 'esqueci a senha' com um e-mail real ATIVO → o e-mail com o código chega; confirmar código + nova senha redefine e o login passa a aceitar a nova senha. E-mail inexistente/inativo → a tela avança igual (sem vazar existência) e nenhum e-mail sai. Reenvio/tentativas têm limite. SMTP ausente cai no log (dev). golangci-lint limpo."
  },
  {
    id: "calendario",
    code: "CAL",
    title: "Calendário de Conteúdo (/calendario)",
    goal: "Agenda de conteúdo por cliente da agência: cada dia mostra o que será entregue (cliente, tipo, status, prioridade, responsável), notas por mês, feriados/datas comemorativas e config na própria página. Dados 100% reais no back (módulo Go calendar), zero mock. Evolução: mídia rica (clicar abre foto/vídeo; fundo do dia com a postagem em grade), perfil estratégico do cliente, página de config + IA de plano mensal (n8n 'Calendar Omni', provider plugável Claude/IAs chinesas). Plano: docs/CALENDARIO_PLAN.md.",
    status: "in_progress",
    estimateWeeks: "Fases 1-6 implementadas (3c-6 em 2026-07-02; pendente validação no dev + ligar workflow n8n); Fase 7 a estimar; WAVE 2 (fases 8-11: drawer de config, chat IA+voz, tasks, realtime) PLANEJADA 2026-07-04 — specs em docs/CALENDARIO_SPECS2.md",
    startedAt: "2026-06-30",
    group: "calendario",
    tasks: [
      { id: "cal-f1-crud", label: "Fase 1 — Back CRUD: migration 0181 (calendar.events + calendar.notes) + módulo Go (model/store/service/http/module) + front integrado (stores/calendar.ts busca eventos por janela from/to, notas por mês com save debounced, CalendarEventForm criar/editar + Editar/Excluir no drawer). Clientes reais via useTenantsStore.", done: true, note: "2026-06-30: migration 0181 aplicada, api rebuildada, módulo registrado + gate /v1/calendar→calendar em app.go. Falta gate de rota/nav próprio no front (hoje preview, workspaceId:'')." },
      { id: "cal-f2-responsaveis", label: "Fase 2a — Responsáveis reais: GET /v1/calendar/responsibles (usuários da conta, subconjunto configurável) + GET /v1/calendar/members + modal de config na página (engrenagem) escolhe quais usuários aparecem. store.people = responsáveis; useCalendarData (mock) REMOVIDO.", done: true, note: "2026-07-01: migration 0182 (calendar.config jsonb por conta) + GET/PUT /v1/calendar/config + CalendarConfigModal.vue. Dados 100% reais." },
      { id: "cal-f2-feriados", label: "Fase 2b — Feriados/datas comemorativas: GET /v1/calendar/holidays?from=&to= computado no back (holidays.go: fixos + móveis via Páscoa/Meeus; conjuntos BR nacional/Sergipe/Aracaju/luxo internacional) + toggles por conjunto na config + render em DayCell/WeekView (faixa --accent-warning). Sem tabela de seed.", done: true, note: "2026-07-01: HolidaysInRange filtra pelos conjuntos ligados na config. Carnaval/Sexta Santa/Corpus/Mães/Pais/Black Friday/Cyber Monday móveis." },
      { id: "cal-f2-skill", label: "Skill de engenharia (< 450 linhas/arquivo): calendar.css virou barrel + calendar/{shell,grid,notes-drawer,week-form}.css; store 560→407 com composables/useCalendarViewport.ts (foco/scroll/rail/nav) + domain/calendar/calendar-api.ts (I/O). Módulo Go todo < 450.", done: true, note: "2026-07-01." },
      { id: "cal-f3-anexos-back", label: "Fase 3 (anexos) BACK — mídia no evento (event.media = MediaItem[]) + anexos avulsos no dia (migration 0183 calendar.day_media): POST /v1/calendar/media (upload multipart → uploads/calendar/{account}/, valida mime+tamanho) + GET/PUT day-media + limite em core.platform_settings (media_limits, GET authed / PUT platform_admin, default vídeo 300MB / imagem 10MB). Espelha tasks/cardapio.", done: true, note: "2026-07-01: media_storage.go + http_media.go + store/service; api rebuildada, migration 0183 aplicada, módulo built. Doc §3.6. DEPLOY: proxy precisa aceitar body 300MB (client_max_body_size)." },
      { id: "cal-f3-anexos-front", label: "Fase 3 (anexos) FRONT — widget de upload (imagem/vídeo) com progresso + preview (thumb/player) + remover, no CalendarEventForm (mídia do post) e no DayDrawer (anexos do dia). Mostra o limite de vídeo e valida no cliente antes de subir.", done: false, note: "2026-07-01: CalendarMediaUploader.vue + useCalendarMedia.ts (XHR c/ progresso) + tipos MediaItem/limits implementados. PENDENTE só validar no dev (HMR, sem npm build ainda)." },
      { id: "cal-f3c-midia-rica", label: "Fase 3c — Mídia rica: clicar em qualquer anexo (do evento e do dia) abre um visualizador (CalendarMediaViewer) — foto em tela cheia + player de vídeo, navegando entre os itens do dia. Poster do vídeo capturado no FRONT (canvas, 1 frame) no upload e enviado como imagem-poster (MediaItem.posterUrl, sem migration/ffmpeg). Fundo do dia (DayCell/WeekView) = mídia da postagem daquele dia, em grade (1 conteúdo = fundo inteiro; 2 = dividido; 3-4 = grid 2×2; +N), com overlay pra legibilidade do número/feriados/chips.", done: false, note: "IMPLEMENTADO 2026-07-02. VALIDAÇÃO DO DONO achou 4 erros reais, CORRIGIDOS na hora: url /uploads relativa quebrava thumb/viewer/fundo no dev (agora resolveMediaUrl+apiBase, padrão do bio); AVIF rejeitado pelo back (whitelist + rebuild); erro de upload seco (agora mostra o motivo do servidor); vídeo antigo sem poster é pulado no fundo (re-subir gera poster). Workflow Calendar Omni IMPORTADO no n8n local. RE-VALIDAR no browser." },
      { id: "cal-f4-perfil-cliente", label: "Fase 4 — Perfil estratégico do cliente (insumo da IA): tabela dedicada 1:1 por account (migration 0185, NÃO em core.accounts) com colunas p/ campos estáveis (segmento, posicionamento, site, instagram, endereço, objetivos, tom de voz) + jsonb flexível (público, oferta, pilares, restrições, histórico, assets). Endpoints GET/PUT client-profile + form no front pra preencher. Doc §3.5.", done: false, note: "IMPLEMENTADO 2026-07-02: migration 0185 APLICADA em dev + profile.go/store_profile.go/http_profile.go (3 endpoints C3, golangci 0 issues) + ConfigClientProfiles.vue na página de config (dirty guard + badge preenchido/vazio). PENDENTE só validar no dev/browser." },
      { id: "cal-f5-config-page", label: "Fase 5 — Página de configuração completa (migra o modal → página /calendario/config com gate de rota próprio): responsáveis + feriados (do modal) + cores por cliente/tipo + início da semana + white-label (logo/cores da visão do cliente) + CONFIG DA IA (provider ativo, modelo, base URL por provider, prompts editáveis, limites) + limites de mídia. Chaves de API server-side. Doc §3.8.", done: false, note: "IMPLEMENTADO 2026-07-02: pages/calendario/{index,config}.vue (modal DELETADO, engrenagem navega); seções ConfigResponsibles/Holidays/Appearance/Ai/MediaLimits; config v2 (C2) sanitizada no back e aplicada no front (weekStartsOn no viewport, clientColors/typeColors nos chips, hexToTriplet). PENDENTE só validar no dev/browser." },
      { id: "cal-f6-ia-n8n", label: "Fase 6 — IA de Calendário + workflow n8n 'Calendar Omni' (§3.7): botão de IA escolhe 1 OU vários clientes + mês → lê perfil+feriados+histórico → gera plano estratégico (pilares/ideias por dia/copy) → SALVA (rascunho) e aplica como notas do mês e/ou eventos. n8n bate na API Go (token de serviço, nunca no Postgres direto). Seletor de provider desde a v1: Claude (padrão) + IAs chinesas via API OpenAI-compatible (DeepSeek/Qwen/Kimi/GLM). Depende da F4 (perfil) + F5 (config da IA).", done: false, note: "IMPLEMENTADO 2026-07-02: migration 0186 APLICADA (calendar.ai_plans) + POST /ai/plan (dispatch goroutine, 503 acionável sem env) + callback público /v1/public/calendar-ai (X-Service-Token constant-time) + CalendarAiPlanModal/Result (polling 3s, aplicar em notas/eventos) + workflow-calendar-omni.json autorado. PENDENTE LIGAR: importar workflow no n8n, criar credentials calendar-ai-*, setar CALENDAR_AI_WEBHOOK_URL/_SERVICE_TOKEN/_CALLBACK_BASE e testar end-to-end (doc automation/CALENDAR_OMNI_WORKFLOW.md)." },
      { id: "cal-f7-whatsapp-share", label: "Fase 7 — Aprovação via WhatsApp (n8n/WAHA) + visão compartilhável read-only para o cliente (respeitando white-label).", done: false },
      { id: "cal-f8-config-drawer", label: "Fase 8 (WAVE 2) — Config em DRAWER lateral com abas (CalendarConfigDrawer sobre OmniEntityDrawer): abas responsáveis/feriados/aparência/IA/clientes/integrações/mídia, 1 aba = 1 modelo de salvar (footer só salva o CalendarConfig; clientes e mídia salvam na aba), dirty-guard único via ui.confirm, deep-link /calendario?config=<aba>, página /calendario/config vira redirect. Doc §3.9; SPEC-F6.", done: false },
      { id: "cal-f9-chat-voz", label: "Fase 9 (WAVE 2) — Chat de IA flutuante + voz: FAB abre CalendarChatPanel; POST /v1/calendar/chat/ask → workflow n8n calendar-chat (AI Agent + Redis Chat Memory por accountId|userId|conversationId); provider GEMINI (free tier AI Studio, OpenAI-compatible) entra no seletor p/ teste; mic (MediaRecorder) → POST /chat/transcribe → workflow calendar-transcribe (whisper-1, nó comprovado do bot WhatsApp) → texto revisável no input; contexto compartilhado entre IAs via GET /v1/runtime/calendar/context (Bearer runtime token). Doc §3.10; SPECs B5-B7/F7/W2-W3.", done: false },
      { id: "cal-f10-tasks-link", label: "Fase 10 (WAVE 2) — Integração Calendário ↔ Tasks: toggle 'Criar task no board' no CalendarEventForm (board/coluna destino na aba Integrações — config tasks.boardId); vínculo via tasks.task_relations (module=calendar) + RelationResolver registrado (modal da task mostra o evento); EventView.taskId (LEFT JOIN, badge/link no drawer do dia); DELETE do evento remove o vínculo via novo RemoveRelation (task não é arquivada); task nasce com uiMetadata.source=calendar. Sem sync bidirecional de status na v1. Doc §3.11; SPECs B5 (seção tasks do C6)/B8/F8.", done: false },
      { id: "cal-f11-realtime-presenca", label: "Fase 11 (WAVE 2) — Realtime + presença + concorrência: canal GET /v1/realtime/calendar (tópico calendar:account:{id}, autorização calendar.view, ticket efêmero) + publisher no módulo calendar (eventos de invalidação event/note/day-media/config/plan); useCalendarRealtime (refetch debounced, nota não sobrescreve draft) + useCalendarPresence (avatares + 'Fulano editando' em notas/evento, molde do useTaskPresence de tasks que JÁ tem presença Google-Docs-like); optimistic locking em calendar.events (migration 0188 version + If-Match → 409 acionável). Doc §3.12; SPECs B9/F9.", done: false },
      { id: "cal-w5-toggle-topo", label: "WAVE 5 (item 1+2) — Toggle 'Criar task' LIGADO por padrão na criação (board configurado) + task nasce no TOPO da coluna (SortOrder menor). Doc CALENDARIO_SPECS5.md.", done: true, note: "IMPLEMENTADO 2026-07-05 (backend+front+n8n; build/vet/golangci/eslint/typecheck limpos; migration 0192; api/web/n8n no ar)." },
      { id: "cal-w5-sync-edicao", label: "WAVE 5 (item 3) — Sync BIDIRECIONAL de edição (título/data-prazo/responsável/cliente) via hook invertido RelationSyncHandler + métodos terminais anti-eco. SPECS5 E2/E4.", done: true, note: "IMPLEMENTADO 2026-07-05 (backend+front+n8n; build/vet/golangci/eslint/typecheck limpos; migration 0192; api/web/n8n no ar)." },
      { id: "cal-w5-deeplink-card", label: "WAVE 5 (item 4) — Deep-link pro CARD específico: TasksBoardView lê ?task=&board= e abre o editor; evento linka /tasks?board=&task=. SPECS5 E6.", done: true, note: "IMPLEMENTADO 2026-07-05 (backend+front+n8n; build/vet/golangci/eslint/typecheck limpos; migration 0192; api/web/n8n no ar)." },
      { id: "cal-w5-espelho", label: "WAVE 5 (item 5) — Espelho BIDIRECIONAL task↔evento (opt-in config.tasks.mirrorTasks): task com prazo vira evento source=task e vice-versa, com guarda anti-loop por source + relation. Migration 0192 (events.source). SPECS5 E1/E3.", done: true, note: "IMPLEMENTADO 2026-07-05 (backend+front+n8n; build/vet/golangci/eslint/typecheck limpos; migration 0192; api/web/n8n no ar)." },
      { id: "cal-w5-sync-status", label: "WAVE 5 (item 6) — Sync de STATUS com mapeamento completo: aba Tasks do config mapeia status de evento ↔ coluna do board, nos dois sentidos (config.tasks.statusColumnMap). SPECS5 E5.", done: true, note: "IMPLEMENTADO 2026-07-05 (backend+front+n8n; build/vet/golangci/eslint/typecheck limpos; migration 0192; api/web/n8n no ar)." },
      { id: "cal-w5-ia-agente", label: "WAVE 5 (item 7) — Chat IA CRIA evento/task no modo propor-e-confirmar: n8n devolve {answer, proposal?}; cartão de confirmação no chat; criação via API autenticada do próprio usuário (SEM service-token escrevendo). SPECS5 E7.", done: true, note: "IMPLEMENTADO 2026-07-05 (backend+front+n8n; build/vet/golangci/eslint/typecheck limpos; migration 0192; api/web/n8n no ar)." },
      { id: "cal-w6-modal-mirror", label: "WAVE 6 (fatia 1) — Fix espelho modal↔card do board: o watch bloqueava TODO o sync quando o draft estava sujo (editar 1 campo travava a atualização externa do cliente etc.). Agora MERGE POR CAMPO (atualiza só os campos não-tocados, preserva os em edição). + remover residual mock do cliente-de-tasks (já usa /v1/tenants real; sobra só o badge de id não-UUID legado).", done: true, note: "IMPLEMENTADO 2026-07-05: mergeExternalTaskChanges no useTasksPageContext (adota campos não-tocados do store no draft sujo). Mock do cliente já era real (/v1/tenants) — só resta o badge de legado não-UUID. eslint/typecheck limpos, web no ar." },
      { id: "cal-w6-inline-calendario", label: "WAVE 6 (fatia 2) — Edição INLINE no calendário: editar campos do evento direto no card/dia (molde do board de tasks), sem abrir modal.", done: true, note: "IMPLEMENTADO 2026-07-06: DayDrawer com selects/input inline (status/prioridade/tipo/cliente/responsável/horário) que salvam via store.updateEvent (full-replace + optimistic locking). eslint/typecheck limpos, web no ar." },
      { id: "cal-w6-hide-empty", label: "WAVE 6 (fatia 3) — Campo vazio não aparece no calendário: só mostra o que tem valor OU foi adicionado via 'Adicionar campo' (molde do modal de tasks; sem 'Não definido' pra tudo).", done: true, note: "IMPLEMENTADO 2026-07-06: DayDrawer esconde cliente/responsável/horário quando vazios + botões '+ campo' que revelam os faltantes (reset por evento). Status/prioridade/tipo têm default, mostram sempre." },
      { id: "cal-w6-upload-por-cliente", label: "WAVE 6 (fatia 4) — Upload ligado ao ITEM do dia: cada anexo do dia aponta a um evento/post (e HERDA o cliente dele), ligando o anexo à task por tabela. PERMITE 'sem item'.", done: true, note: "IMPLEMENTADO 2026-07-06 (pivot de 'por cliente' → 'por evento'): MediaItem.eventId + clientId (back jsonb; normalizeMedia valida UUID/vazio). CalendarMediaUploader com prop items — seletor por miniatura ('Dia (sem item)' + eventos do dia); escolher o evento seta eventId + herda clientId. DayDrawer passa dayItemOptions (eventos do dia). eslint/build limpos, no ar." },
      { id: "cal-w6-media-refinos", label: "WAVE 6 (fatia 4b) — Refinos do anexo por item: 'Mídia do post' do evento mostra a UNIÃO (mídia do evento + anexos do dia apontados a ele); indicador 📎 com contagem em cada item da lista do dia.", done: true, note: "IMPLEMENTADO 2026-07-06: DayDrawer com activeEventMedia (união) + eventAnexoCount (badge 📎). Front only, no ar." },
      { id: "cal-w6-cross-media-a", label: "WAVE 6 (fatia 5, cruzamento A) — Mídia do calendário aparece na task (espelho read-only): a mídia do evento vinculado é exibida no modal da task, sem duplicar arquivo, preservando o isolamento entre contas.", done: true, note: "IMPLEMENTADO 2026-07-06: back espelha em ui_metadata.calendarMedia (eventMediaForTask; syncTaskFromEvent no update do evento + syncEventMediaToTask no PutDayMedia; whitelist normalizeCalendarMediaMetadata só /uploads/calendar/). Front: TaskCalendarMediaItem + seção 'Mídia do calendário' read-only no modal. Sem migration (ui_metadata jsonb). eslint/build limpos, no ar." },
      { id: "cal-w6-cross-media-b", label: "WAVE 6 (fatia 5, cruzamento B) — Vídeo da task aparece no calendário (espelho read-only o outro sentido).", done: true, note: "IMPLEMENTADO 2026-07-06: coluna calendar.events.linked_media (jsonb, migration 0193) + EventView.linkedMedia. TaskSyncSnapshot.Media (MediaSnapshot neutro) carrega os vídeos da task (dispatchTaskSync ← ui_metadata.videos); applyTaskSyncToEvent/maybeCreateMirrorEvent gravam via SetEventLinkedMedia (terminal, sem loop). Front: CalendarEvent.linkedMedia entra na união da 'Mídia do post' do DayDrawer. go build/vet/test + Vite limpos, no ar." },
      { id: "cal-w6-shared-taxonomy", label: "WAVE 6 (fatia 6) — Tipos e status COMPARTILHADOS entre Calendário e Tasks: os dois módulos oferecem os mesmos tipos e status (fonte única).", done: true, note: "IMPLEMENTADO 2026-07-06: ~/utils/content-taxonomy.ts (CONTENT_TYPES/STATUSES/PRIORITIES) vira fonte única. calendar.ts DERIVA EVENT_TYPE_META/STATUS_META/PRIORITY_META dela; tasks usa a mesma lista no seletor de tipo (lidera com os 6 tipos + legados) e nas colunas padrão de board novo (= os 6 status). Board existente mantém colunas; statusColumnMap faz a ponte. eslint/build limpos, no ar." },
      { id: "cal-w6-day-collapse", label: "WAVE 6 (fatia 7) — Cada item do dia vira um COLLAPSE (accordion) no painel do dia.", done: true, note: "IMPLEMENTADO 2026-07-06: DayDrawer reestruturado — cada evento do dia é um header clicável (chevron/cor/título/cliente·hora/📎/status) que abre o corpo (detalhe + edição inline + mídia) só no item ativo; um aberto por vez (toggleItem). Substitui o padrão 'ativo no topo + lista'. eslint/Vite limpos, no ar." },
      { id: "cal-w6-realtime-reload", label: "WAVE 6 (bug) — Calendário desatualizado ao mudar algo pela task (precisava recarregar / responsável não mudava).", done: true, note: "CORRIGIDO 2026-07-06: raiz = store.init() é guardado (roda 1×), então voltar ao /calendário não refazia o fetch (WS do calendário só entrega com a página aberta; mexer na task era no board). Fix: refetchWindow ao (re)entrar na página (onMounted quando já inicializado + onActivated p/ keepalive). O back já sincronizava responsável/data (applyTaskSyncToEvent); faltava o front refetchar." },
      { id: "cal-w6-mirror-tz", label: "WAVE 6 (bug) — Task com data (IA) espelhava no calendário no DIA ANTERIOR.", done: true, note: "CORRIGIDO 2026-07-06: task data-only nasce meia-noite UTC; o mirror fazia .In(saoPauloLoc) (−3h) → dia anterior. Helper mirrorEventDateTime: meia-noite UTC = 'sem hora' → data em UTC (dia inteiro); hora real converte pra SP. Aplicado em maybeCreateMirrorEvent + applyTaskSyncToEvent. Confirma: task com data já cria no calendário sozinha (não precisa a IA criar evento à parte)." },
      { id: "cal-w6-notes-sidebar", label: "WAVE 6 (UX) — Coluna de anotações MINIMIZÁVEL (vira sidebar slim: mês vertical + setas + clicar p/ abrir).", done: true, note: "IMPLEMENTADO 2026-07-06: botão nos CalendarControls colapsa a leftcol num strip de 2.75rem (nome do mês vertical clicável + setas ↑↓ de mês + expandir). Persistido em localStorage. Front only." },
      { id: "cal-w6-notask-badge", label: "WAVE 6 — Badge 'evento sem task' + botão criar task manual quando o item do calendário não tem task.", done: true, note: "IMPLEMENTADO 2026-07-06: DayDrawer mostra badge âmbar 'Sem task' + botão 'Criar task' quando event.taskId vazio. Endpoint POST /v1/calendar/events/{id}/task (CreateTaskForEvent reusa createLinkedTask C10, idempotente). store.createTaskForEvent + refetch (badge some)." },
      { id: "cal-w6-delete-policy", label: "WAVE 6 — Exclusão de evento/task com par: PERGUNTAR na hora (excluir os dois ou só um).", done: true, note: "IMPLEMENTADO 2026-07-06 (decisão do dono = perguntar na hora): excluir evento → confirma → se tem task, 2º modal 'arquivar a task também?'. Back: DELETE events/{id}?archiveTask=true arquiva a task (archiveLinkedTask, tolera 404 do espelho já apagado). Falta simétrico task→evento no board (follow-up)." },
      { id: "cal-w6-ai-reads-notes", label: "WAVE 6 — IA/chat lê as anotações do mês (contexto do que trabalhar no mês).", done: true, note: "JÁ FUNCIONAVA (verificado 2026-07-06): back BuildAIContext faz GetNotes(month) → context.monthNotes; n8n 'Montar contexto' injeta no system prompt ('Notas do mes... use como contexto'). activeNotesMonthKey === focusMonthKey (chat manda o mês exibido). Zero código novo." },
    ],
    blockers: [],
    verifiable: "/calendario com sessão real: eventos/notas/responsáveis/feriados vêm do back (Network mostra /v1/calendar/*, nunca mock). Criar/editar/excluir evento persiste; engrenagem abre config e salva responsáveis + toggles de feriado por conta; feriados aparecem no dia certo (inclusive móveis). Cada arquivo do módulo < 450 linhas."
  },
  {
    id: "liquid-glass-ui",
    code: "GLASS",
    title: "Conversão da UI para Liquid Glass",
    goal: "Levar a estética liquid glass do /calendario (aurora ambiente + superfícies de vidro que a refratam + botões flutuantes) para todo o painel, como um TEMA PRÓPRIO 'Liquid Glass' (5º tema, ao lado de light/dark/apple/custom) selecionável e persistido — mantendo Original/Padrão intactos. DECISÕES do dono (2026-07-02): (1) Liquid Glass é um tema próprio, não um modificador; (2) a persistência de tema/aparência sai do acoplamento com o módulo `queue` e vira um MÓDULO `theme` separado, GLOBAL da plataforma (resolve o bug do Page Headers que não persiste pro platform_admin de tenant vazio). Já implementado como preview sempre-ligado: aurora movida do /calendario pro shell (`.module-workspace-full` + `.workspace` no `layouts/dashboard.vue`, z-index:-1). Falta: criar o módulo `theme` (Go, global), o tema Liquid Glass de verdade, gatear a aurora/vidro nele, e converter as páginas em superfícies de vidro — sempre com tokens do design system (nunca hex). Plano canônico: docs/THEME_MODULE_PLAN.md.",
    status: "in_progress",
    estimateWeeks: "a estimar (incremental, página a página)",
    startedAt: "2026-07-02",
    group: "ui-liquid-glass",
    tasks: [
      { id: "glass-aurora-global", label: "Passo 1 — Mover a aurora ambiente do /calendario para o shell global: remover `.calendar-page::before` + `@keyframes calendar-aurora` + a regra prefers-reduced-motion de `assets/styles/calendar/shell.css` e recriar como `.module-workspace-full::before` (z-index:-1, atrás do conteúdo, sem tocar cada página) no `layouts/dashboard.vue`. Só tokens do tema (--primary/--primary-600/--success/--danger), respeita prefers-reduced-motion.", done: true, note: "FEITO 2026-07-02: aurora removida de shell.css do calendário. Não ficou 'sempre-ligada' no shell — virou parte do TEMA `.theme-liquidglass` (omni-tokens.css), ligada/desligada pelo Theme Studio (ver glass-theme-liquidglass). Cards de vidro do /calendário seguem sempre, refratando a aurora quando o tema está ativo." },
      { id: "glass-operacao-workspace", label: "Estender a aurora ambiente ao container `.workspace` (páginas da Operação/Fila usam esse wrapper no dashboard.vue, não o `.module-workspace-full`) — ou unificar os dois containers para não duplicar a camada.", done: true, note: "FEITO 2026-07-02: aurora cobre os dois containers via `.theme-liquidglass .module-workspace-full::before, .theme-liquidglass .workspace::before` (omni-tokens.css); `overflow:hidden` no `.workspace` também gated por `.theme-liquidglass` (só contém a camada quando o tema está ativo, sem afetar a /operacao nos outros temas). PENDENTE validar no browser com Liquid Glass ativo que o scroll da /operacao segue ok." },
      { id: "glass-theme-persist-diag", label: "DIAGNÓSTICO (2026-07-02) da persistência de tema/Page Headers: o appearance (tema + overrides, incl. os toggles de Page Header eyebrow/title/description) É gravado no banco, MAS dentro do módulo `queue/settings` (tenant-scoped, `AppearanceSectionRecord`), via `PATCH /v1/settings/appearance`. O front (`useOmniTheme`) só persiste no banco quando `isRemoteManaged` = autenticado COM `activeTenantId` não-vazio; senão cai em localStorage. Como `platform_admin` tem TenantID vazio (ver memória operacao_scope_account) e contas sem o módulo `queue` não carregam `/v1/settings`, o tema deles só vai pra localStorage — e some quando o admin entra em 'ver como' um cliente com tenant (dispara `clearLocalThemeStorage`), trocando de navegador/device ou limpando cache. ESSE é o motivo de 'Page Headers volta e meia'. Além disso `normalizeAppearanceTheme` no back só aceita light|dark|apple|custom (um tema novo 'liquidglass' não seria persistido as-is).", done: false, note: "Só documentação do diagnóstico; a correção é o item glass-theme-persist." },
      { id: "glass-theme-module-back", label: "Appearance GLOBAL no back — desacoplar do `queue/settings`. Concern próprio (`platform_appearance_{model,service,http}.go`) DENTRO do módulo `core` (config global de plataforma, como o `menu_layout`; NÃO um registry-module toggleável por conta), reusando `core.platform_settings` (chave `appearance`) e `PostgresPlatformSettingsRepository`. `GET /v1/platform/appearance` (RequireAuth) + `PUT` (platform_admin). SEM enum fechado de tema: valida `activeTheme` como slug (`^[a-z][a-z0-9-]*$`) — catálogo de temas é fonte de verdade no front. SEM migration (tabela 0160 já existe). Rebuild: `docker compose up -d --build api`.", done: true, note: "FEITO 2026-07-02: platform_appearance_{model,service,http}.go no core (reusa PostgresPlatformSettingsRepository, chave 'appearance') + wiring no module.go + GET/PUT /v1/platform/appearance; api rebuildada (build exit 0). Decisões: global (não Fila) + slug em vez de enum. Plano: docs/THEME_MODULE_PLAN.md." },
      { id: "glass-theme-front-wire", label: "Front: `useOmniTheme` lê/grava no módulo `theme` (GET/PUT global) em vez do `/v1/settings/appearance` do queue; remove a dependência de `activeTenantId` não-vazio (por isso o platform_admin persiste agora). Page Headers (que a UI chama de 'controle GLOBAL') passa a salvar de verdade. Preferência pessoal light/dark do usuário continua como conveniência local.", done: false, note: "IMPLEMENTADO 2026-07-02 (eslint 0 erros + vue-tsc sem erro novo): isRemoteManaged (tenant) → isAuthenticated (ler) + canManageAppearance=platform_admin (gravar); loadRemoteAppearance() GET no boot/login; persist PUT /v1/platform/appearance. Corrige 'Page Headers volta e meia'. PENDENTE validar no browser (sem npm build). Catálogo extraído p/ omni-theme-catalog.ts (useOmniTheme 795→<500 linhas)." },
      { id: "glass-theme-liquidglass", label: "Tema 'Liquid Glass' de verdade: `liquidglass` no catálogo (defaults/paleta própria + selectorClass `.theme-liquidglass` + label) + bloco `.theme-liquidglass` (paleta escura + tokens de vidro `--glass-*`) no omni-tokens.css + aurora GATEADA por `.theme-liquidglass` (saiu de sempre-ativa no dashboard.vue) + Liquid Glass na lista do Theme Studio. Mantém light/dark/apple/custom intactos.", done: false, note: "IMPLEMENTADO 2026-07-02 (lints verdes). Com outros temas: sem aurora. Liquid Glass também no SWITCH RÁPIDO do header (DashboardThemeSwitcher: light/dark/liquidglass + Theme Studio), não só no Studio. Header vira vidro via tokens `--admin-header-*` sob `.theme-liquidglass` (o shell do header já aplica backdrop-filter). PENDENTE validar no browser: ativar Liquid Glass e ver aurora + header + contraste; ajustar paleta/tokens se preciso." },
      { id: "glass-tokens", label: "Tokenizar o vidro no design system: tokens `--glass-*` (blur, saturação, alpha da superfície, borda, highlight) a partir do que o /calendario já usa, para não repetir CSS em cada página e evitar drift.", done: true, note: "FEITO 2026-07-02: `--glass-blur/--glass-saturate/--glass-surface-alpha/--glass-border/--glass-highlight` no bloco `.theme-liquidglass` (omni-tokens.css)." },
      { id: "glass-superficie-componente", label: "Superfície de vidro reutilizável (`.omni-glass`) que qualquer card/painel usa: sólida nos temas normais, vidro (backdrop-filter) só sob `.theme-liquidglass`, com fallback `@supports` (surface sólida translúcida) e texto em `--text` (contraste).", done: false, note: "IMPLEMENTADO 2026-07-02 (lints verdes): `.omni-glass` base em components.css + regras de vidro/fallback em omni-tokens.css. PENDENTE validar no browser." },
      { id: "glass-botoes-flutuantes", label: "Botões flutuantes translúcidos: padronizar FABs (o `.dashboard-feedback-btn`) e barras de ação flutuantes no estilo glass, reusando os tokens do design system.", done: false, note: "IMPLEMENTADO 2026-07-02: `.dashboard-feedback-btn` tokenizado (removido o hex `#3b82f6` → `rgb(var(--primary))`) + variante de vidro sob `.theme-liquidglass`. PENDENTE validar no browser." },
      { id: "glass-rollout-paginas", label: "Rollout página a página (CRM, Tasks, ERP, Cardápio, Manage, Meta Ads, etc.): aplicar as superfícies de vidro nos cards/painéis. Cada página validada no browser (tema claro E escuro + tela pequena) antes de marcar. Não remover funcionalidade existente pra 'encaixar' o visual.", done: false, note: "INICIADO 2026-07-02: (1) `.metric-card` compartilhada (CRM Resumo + grids de métrica) via `.theme-liquidglass .metric-card`; (2) HEADER via tokens `--admin-header-*`; (3) piloto /automacao — status bar `.asb`, QR `.aw-qr` e os 5 cards de seção (`.abh/.asc-card/.am-section/.kd-section/.ctx-card`) via regra `html.theme-liquidglass ...` (prefixo html vence o `[data-v]` scoped sem editar componente, reusando tokens). PADRÃO: página existente = regra html-prefixada reusando tokens; componente NOVO = classe `.omni-glass`. PENDENTE validar no browser e seguir para Tasks/ERP/Manage/Cardápio/Meta Ads." },
      { id: "glass-perf-a11y", label: "Perf + acessibilidade: backdrop-filter é caro — medir (evitar dezenas de camadas de blur sobrepostas na mesma tela), garantir prefers-reduced-motion (aurora parada) e contraste AA do texto sobre o vidro. Registrar achados de perf conforme ENGINEERING_PRINCIPLES.md.", done: false },
      { id: "glass-docs", label: "Sincronizar os 3 docs conforme evolui: DESIGN_SYSTEM.md (tokens/superfície de vidro) + AGENT.md dos módulos tocados + este roadmap. Já anotado no calendar/AGENT.md que a aurora ambiente vive no shell (dashboard.vue), não mais no /calendario.", done: false },
    ],
    blockers: [],
    verifiable: "No browser: toda página do painel (via `.module-workspace-full`) mostra a aurora ambiente sutil ao fundo e o /calendario continua idêntico (seus cards de vidro agora refratam a aurora global) — passo 1 concluído. A conversão das demais páginas em superfícies de vidro segue incremental, cada uma validada no claro/escuro/mobile, sempre com tokens do design system (sem hex hardcoded)."
  },
  {
    id: "ac-fixes-2026-07",
    code: "AC",
    title: "Correções do Diagnóstico 2026-07",
    goal: "Aplicar as correções priorizadas da auditoria de manutenção/segurança/performance de docs/relatorios/2026-07: fechar riscos P0 (cache do resolvedor principal, runbooks de VPS/segredos), endurecer banco/backup, paginar relatórios pesados, iniciar o recorte do front, e subir observabilidade/limites básicos. Cada item tem spec própria em docs/specs/ac-fixes-2026-07/.",
    status: "in_progress",
    estimateWeeks: "diagnóstico fechado em 03/07 (deploy em prod); restam prevenção + off-site + recorte 2",
    group: "ac-fixes-2026-07",
    tasks: [
      { id: "ac-01-principalcache", label: "AC-01 [P0] — Ligar o PrincipalCache: ativar/instanciar o cache do resolvedor principal (hoje resolvendo o principal a cada request) para tirar a carga repetida do caminho de autenticação/autorização. Spec: docs/specs/ac-fixes-2026-07/AC-01.md.", done: true, note: "LIVE em prod 2026-07-03 (deploy local-20260703-101734): principal_cache_stats com ~89% de hit rate medido na VPS." },
      { id: "ac-02-runbook-stacks-orfas", label: "AC-02 [P0] — Runbook das stacks órfãs da VPS: documentar (e checar) quais stacks/containers órfãos existem na VPS (omni-crow/omni-staging/omnichannel-mvp), o que NÃO pode ser parado (Caddy/painel) e o procedimento seguro de limpeza. Spec: docs/specs/ac-fixes-2026-07/AC-02.md.", done: true, note: "Diagnóstico executado: órfãs já paradas (exited, CPU ~0%), Caddy intocado; runbook na spec." },
      { id: "ac-03-runbook-dump-segredos-tmp", label: "AC-03 [P0] — Runbook de dump + segredos em tmp/: procedimento para gerar/restaurar dump com segurança e higienizar segredos deixados em tmp/ (rotação + remoção), fechando o vazamento de credenciais em arquivos temporários. Spec: docs/specs/ac-fixes-2026-07/AC-03.md.", done: true, note: "tmp/ higienizado (dump de 245MB e segredos removidos — diretório vazio conferido em 2026-07-03); runbook na spec." },
      { id: "ac-04-role-postgres-least-privilege", label: "AC-04 [P1] — Role Postgres least-privilege: criar role não-superuser para a app conectar (pré-requisito também do RLS, hoje no-op por conectar como SUPERUSER), com grants mínimos por schema. Spec: docs/specs/ac-fixes-2026-07/AC-04.md.", done: true, note: "LIVE em prod 2026-07-03: pg_stat_activity com 5 conexões omni_app; superuser só no migrate. COM INCIDENTE: deploy antes da role/envs na VPS = ~1h de 502 (registro de falhas nº 13 do ENGINEERING_PRINCIPLES) — prevenção = ac-04b." },
      { id: "ac-04b-migrate-auto-provision-role", label: "AC-04b [P1] — Auto-provisionar a role omni_app no migrate (prevenção do outage de 2026-07-03): o `migrate up` passa a criar a role a partir de DATABASE_APP_URL no boot (CREATE ROLE IF NOT EXISTS + ALTER ROLE PASSWORD + GRANT CONNECT, idempotente, rodando como superuser), com guarda-corpo de falha alta e cedo se a senha faltar em prod. Hoje criar a role é passo manual de runbook; esquecer derruba tudo (api crash-loop 28P01 → web não sobe → 502). Torna o deploy AC-04 self-healing. Ver docs/MULTITENANT_COMPLETION_PLAN.md (AC-04) e docs/DEPLOY_VPS.md.", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/AC-04b-migrate-auto-provision-role.md — despachar 1 agente opus." },
      { id: "ac-05-backup-agendado-offsite", label: "AC-05 [P1] — Backup agendado + off-site: agendar backup do Postgres e enviar cópia para armazenamento off-site (fora da VPS), com verificação de restauração. Spec: docs/specs/ac-fixes-2026-07/AC-05.md.", done: false, note: "PARCIAL: cron diário 06:40 UTC + retenção 7d/4sem LIVE na VPS desde 03/07. FALTA: off-site (rclone) + drill de restore mensal — é o que mantém a task aberta. Spec da pendência: docs/specs/ac-fixes-2026-07/AC-05b-offsite-restore-drill.md." },
      { id: "ac-06-paginacao-reports-analytics-bi", label: "AC-06 [P1] — Paginação em reports/analytics/BI: paginar (limit/offset ou cursor) os endpoints de relatórios/analytics/BI que hoje varrem tudo, evitando respostas gigantes e queries sem limite. Spec: docs/specs/ac-fixes-2026-07/AC-06.md.", done: true, note: "LIVE em prod 2026-07-03: teto de paginação em reports/analytics/BI, shape das respostas preservado." },
      { id: "ac-07-refactor-front-recorte-1", label: "AC-07 [P1] — Refactor do front, recorte 1: primeiro lote de arquivos front acima do limite de linhas (skill < 450) quebrados em composables/subcomponentes, sem mudar comportamento. Spec: docs/specs/ac-fixes-2026-07/AC-07.md.", done: true, note: "Recorte 1 entregue: components.css fatiado em 11 arquivos + roadmap-data.ts dividido em data/*. Os 105 arquivos .vue/.ts gigantes são o ac-07b (recorte 2)." },
      { id: "ac-07b-refactor-front-recorte-2", label: "AC-07b [P1] — Refactor do front, recorte 2: os 105 arquivos .vue/.ts acima de 450 linhas (recorte 1 cobriu CSS e dado declarativo). Fatiar por responsabilidade, sem mudar comportamento, priorizando os arquivos que mais recebem mudança no dia a dia.", done: false, note: "Specs prontas (2026-07-03): docs/specs/ac-fixes-2026-07/AC-07b-onda-2a..2f (6 ondas por área; 2f é spec-molde em 8 lotes). Ordem: 2a→2b→2c→2d→2e→2f; calendário FORA." },
      { id: "ac-11-limites-healthchecks-compose", label: "AC-11 [P1] — Limites + healthchecks no compose: definir limites de recursos (mem/cpu) e healthchecks nos serviços do docker-compose, para conter crash-loops que comem CPU do host e detectar containers doentes. Spec: docs/specs/ac-fixes-2026-07/AC-11.md.", done: true, note: "LIVE em prod 2026-07-03: docker inspect confirma api mem 512MB / web 768MB + healthchecks ativos." },
      { id: "ac-12-finance-api-go", label: "AC-12 [P1] — Finance na API Go: mover a lógica de finance (hoje fora do backend canônico) para a API Go como serviço de domínio, com fonte única no banco. Spec: docs/specs/ac-fixes-2026-07/AC-12.md.", done: true, note: "LIVE em prod 2026-07-03: módulo finance + migration 0187 aplicada no boot; web/server/ (BFF mock) removido." },
      { id: "ac-15-testes-front-onda-1", label: "AC-15 [P1] — Testes de front, onda 1: primeira onda de testes automatizados no front (composables/util críticos e fluxos-chave), cobrindo o que o recorte 1 (AC-07) tocar. Spec: docs/specs/ac-fixes-2026-07/AC-15.md.", done: true, note: "Onda 1 entregue: 144 testes verdes. Expansão = ac-15b (onda 2)." },
      { id: "ac-15b-testes-front-onda-2", label: "AC-15b [P2] — Testes de front, onda 2: expandir a cobertura dos 144 testes da onda 1 para os fluxos-chave restantes (operação, cardápio, calendário), acompanhando o recorte 2 (ac-07b).", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/AC-15b-testes-onda-2.md — rodar APÓS a onda 2c do ac-07b." },
      { id: "ac-16-monitoracao-minima", label: "AC-16 [P1] — Monitoração mínima: instrumentação/observabilidade mínima (health, métricas básicas de API/banco e alerta simples) para enxergar problemas antes do usuário. Spec: docs/specs/ac-fixes-2026-07/AC-16.md.", done: true, note: "LIVE: /healthz com ping de banco em prod + check-vps.sh no cron do host. Evolução (uptime externo, fan-out, anomalia) = fase observabilidade-n8n (OBS-01..06)." },
      { id: "ac-17-load-test-explain", label: "AC-17 [P2] — Load test + EXPLAIN: primeira medição sob carga (k6 ou similar contra build de prod em staging/local) + EXPLAIN ANALYZE nos endpoints de lista mais pesados. O diagnóstico 2026-07 foi 100% estático — nota de performance ≥9 exige prova sob carga.", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/AC-17-load-test-explain.md — k6 via docker, SÓ contra build local (guarda-corpo anti-VPS)." },
    ],
    blockers: [],
    verifiable: "Cada item AC-* fechado quando sua spec em docs/specs/ac-fixes-2026-07/ é atendida e verificada: P0 (AC-01/02/03) removem o risco imediato (cache ligado no runtime, runbooks executáveis, segredos rotacionados/higienizados); P1 endurecem banco/backup, paginam os relatórios pesados, iniciam o recorte + testes do front e sobem limites/healthchecks/monitoração. Referência do diagnóstico: docs/relatorios/2026-07."
  },
  {
    id: "observabilidade-n8n",
    code: "OBS",
    title: "Observabilidade & Alertas (n8n)",
    goal: "Fechar a monitoração do Omni usando o n8n como orquestrador de alertas (evolução do AC-16 do diagnóstico docs/relatorios/2026-07): plugar as sondas que já existem (check-vps.sh no host, /healthz na API) em webhooks n8n, fazer fan-out multi-canal (e-mail/Telegram/ntfy), somar um monitor externo de uptime, detecção de anomalia de métricas e alertas de negócio, tudo visível num painel de status interno.",
    status: "pending",
    estimateWeeks: "a estimar (incremental, por prioridade P1 → P2)",
    group: "observabilidade-n8n",
    tasks: [
      { id: "obs-01-check-vps-webhook-n8n", label: "OBS-01 [P1] — Ligar os alertas do check-vps.sh (host) a um webhook n8n: fazer o check-vps.sh disparar para um webhook n8n quando detectar problema, tirando o alerta do log local e levando pro fluxo de notificação. Nasce do AC-16 (docs/relatorios/2026-07).", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/OBS-01-check-vps-webhook-n8n.md." },
      { id: "obs-02-fanout-email-telegram-ntfy", label: "OBS-02 [P1] — Workflow n8n de fan-out de alertas: um fluxo n8n único que recebe o alerta e distribui por e-mail + Telegram + ntfy, com deduplicação/severidade para não spammar. Nasce do AC-16 (docs/relatorios/2026-07).", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/OBS-02-n8n-fanout-alertas.md — contrato de payload com OBS-01." },
      { id: "obs-03-uptime-externo-healthz", label: "OBS-03 [P1] — Monitor de uptime externo no /healthz: UptimeRobot (ou BetterStack) checando o /healthz de fora da VPS, para pegar queda total quando o próprio host não consegue avisar. Nasce do AC-16 (docs/relatorios/2026-07).", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/OBS-03-uptime-externo.md (runbook puro)." },
      { id: "obs-04-deteccao-anomalia-metricas", label: "OBS-04 [P2] — Detecção de anomalia de métricas no n8n: o n8n cruza RAM/CPU/erros 5xx/latência e avisa quando o comportamento foge do padrão, indo além do threshold fixo. Nasce do AC-16 (docs/relatorios/2026-07).", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/OBS-04-anomalia-n8n.md — DEPENDE de OBS-02 e OBS-05." },
      { id: "obs-05-painel-status-interno", label: "OBS-05 [P2] — Painel de status interno: página interna consumindo os eventos de monitoração (up/down, últimos alertas, métricas) para leitura rápida do estado do sistema sem depender do canal de notificação. Nasce do AC-16 (docs/relatorios/2026-07).", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/OBS-05-painel-status-interno.md — módulo Go monitoring novo + /monitoramento real (mata o mock)." },
      { id: "obs-06-alertas-de-negocio", label: "OBS-06 [P2] — Alertas de negócio via n8n: sinais de domínio (fila parada, pedido travado, backup falhou, etc.) disparando o mesmo fluxo de fan-out, para pegar problema de operação e não só de infra. Nasce do AC-16 (docs/relatorios/2026-07).", done: false, note: "Spec pronta (2026-07-03): docs/specs/ac-fixes-2026-07/OBS-06-alertas-negocio.md — fase 1: long_queue_wait + long_pause (emissores + processadores + migration)." },
    ],
    blockers: [],
    verifiable: "Provado quando um alerta real percorre o fluxo ponta a ponta: P1 — check-vps.sh e o monitor externo do /healthz disparam o webhook n8n, e o fan-out entrega no e-mail, no Telegram e no ntfy (OBS-01/02/03); P2 — a detecção de anomalia acusa desvio de RAM/CPU/5xx/latência, o painel de status interno reflete os eventos e um alerta de negócio (fila parada/pedido travado/backup falhou) chega pelo mesmo fluxo (OBS-04/05/06). Referência do diagnóstico: docs/relatorios/2026-07 (AC-16)."
  }
];
